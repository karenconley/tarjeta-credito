//preguntar por número de tarjeta:
var creditCard = prompt("Ingrese su número de tarjeta");

function number (){
  var result = [creditCard];
  //se recorre el número ingresado
  for (var i = 0; i < creditCard.length; i++);
  result.push(creditCard[i]);
  if(numberCard !== 'number' || numberCard === 0){
}
var newResult = result.reverse();
console.log(newResult);
}
